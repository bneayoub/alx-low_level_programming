/**
 * _islower - _putchar alphabets
 * @n: the number to be checked
 * Return: 0
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	if (n < 0)
	{
		return (-1 * n);
	}
	else
	{
		return (0);
	}
}

#include "main.h"

/**
 * _islower - _putchar alphabets
 * @n: the number to be checked
 * Return: 0
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}

	else
	{
		return (0);
	}
}
